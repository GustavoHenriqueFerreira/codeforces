#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int N, D;

    cin >> N >> D;
    vector<int> lugares;

    for (int i = 0; i < N; i++) {
        cin >> lugar;
        lugares.push_back(lugar);
    }

    if (lugares.size() > 1) {

    }

    return 0;
}